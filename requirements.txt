# Core dependencies
flask>=3.0.0
# Pin below the 1.x major: anthropic 1.0.0 removed the `temperature`/`top_p`/`top_k`
# sampling kwargs (TypeError if passed), which core/llm_client.py passes on every
# messages.create() call — an unpinned bump to 1.0.0 broke ALL LLM responses (chat +
# content) with the "I apologize, but I encountered an error" fallback. Staying on 0.x
# restores it. Moving to 1.x is a deliberate migration (drop temperature, httpx→httpx2,
# Python >=3.10) — do that on its own branch, not via an unpinned resolve.
anthropic>=0.40.0,<1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
gunicorn>=21.0.0           # Production WSGI server
flask-cors>=4.0.0          # CORS for web API access
# supabase>=2.0.0          # Not needed — using edge function proxy via requests

# Google Auth
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
openpyxl>=3.1.0                      # parse DOB NOW .xlsx objection exports (Drive poller uses REST, not google-api-python-client)

# RAG / Vector Store
pinecone>=5.0.0
voyageai>=0.3.0          # Anthropic-recommended embeddings
pymupdf>=1.24.0          # PDF processing
python-docx>=1.1.0       # Google Chat export parsing

# Optional: Alternative embedding providers
# openai>=1.0.0          # If using OpenAI embeddings instead of Voyage

# Development dependencies
pytest>=8.0.0
pytest-cov>=4.1.0

# Type checking (optional)
mypy>=1.8.0
types-requests>=2.31.0
beautifulsoup4
httpx>=0.24.0
